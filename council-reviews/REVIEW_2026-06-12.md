# Council Review — the-council (2026-06-12)

**Target:** `github.com/adnanraf1q/the-council` @ `cbbc18a` (self-review, first session of the council)
**Stated goal:** portable, machine-agnostic Claude Code skills; one shareable source of truth across machines, accounts, and teams.

> Severity: 🔴 high · 🟡 med · 🟢 low

## Executive summary

**Verdict: invest** (trivially — it's a day old, cheap to maintain, and already deployed to one machine). The repo does what it says: the skill is verified machine-agnostic and the install paths are real. The gaps are all about *sharing* — the thing the repo exists for: no license (so "shareable" is legally false), no staleness signal for copied deployments, and a security posture that hasn't been thought through for the symlink-auto-update convention it recommends.

| # | Action | Sev | Effort | Why | Status |
|---|---|:--:|---|---|---|
| 1 | Add a LICENSE (MIT) | 🔴 | quick-win | Public repo with no license = all-rights-reserved; colleagues copying `council/` into work repos have no legal right to. | Open |
| 2 | Add a multi-project guard to SKILL.md | 🟡 | quick-win | Invoked from a directory of 15 projects, the skill assumes cwd is the app instead of asking which one. | Open |
| 3 | Declare SKILL.md canonical | 🟡 | quick-win | The 8-role panel is defined twice (README table + SKILL.md); one sentence ends the race. | Open |
| 4 | Pin-by-commit advice for work machines | 🟡 | quick-win | Symlink auto-update means a compromised account pushes executing instructions to every linked machine. | Open |
| 5 | Frontmatter lint CI (name == folder, parseable YAML) | 🟡 | medium | A malformed SKILL.md doesn't error — the skill silently never appears. | Open |

---

## Engineering panel

### Architect

| Sev | Effort | Finding | Where |
|:--:|---|---|---|
| 🟡 | quick-win | **Panel definition duplicated** — two sources of truth for the same 8 roles; the first one-sided edit starts the drift the Conventions section warns about. | `README.md:64-73` · `council/SKILL.md:21-58` |
| 🟢 | quick-win | **"Adding a new skill" requires manual catalog sync** — same drift class, pre-installed for every future skill. | `README.md:96-109` |
| 🟢 | — | **Path/symlink stutter** — `the-council/council/` repeats in paths and `ln -s` targets; cosmetic, not worth a rename. | `README.md:42` |

### Security reviewer

| Sev | Effort | Finding | Where |
|:--:|---|---|---|
| 🟡 | quick-win | **Supply-chain exposure by design** — `git pull` updates every linked project at once; anyone who can push to main can change what Claude Code does with the operator's permissions. Likelihood low, blast radius high. Mitigate by pinning a commit. | `README.md:48` |

*No other findings — repo is markdown only, no secrets, verified by grep audit this session.*

### Performance, infra & scale

*Skipped — no runtime, no data path. The one scale-relevant property is already right: the skill runs inline (no subagent fan-out) and SKILL.md is 64 lines, so its per-session context cost is negligible.*

### Reliability (operator + QA)

| Sev | Effort | Finding | Where |
|:--:|---|---|---|
| 🟡 | quick-win | **Copied deployments have no staleness signal** — no version in frontmatter or CHANGELOG, so a stale copied `council/` looks identical to current. | `council/SKILL.md` |
| 🟡 | medium | **The ONE gate worth adding: frontmatter lint** — assert each `SKILL.md` has parseable YAML and `name` == folder; malformed frontmatter fails silently (the skill just never appears). | `.github/` (none yet) |
| 🟢 | — | **Windows `xcopy` fallback untested** on a real Windows box. Flag, don't fix. | `README.md:46` |

---

## Product panel

### Product & market *(merged)*

| Sev | Effort | Finding | Where |
|:--:|---|---|---|
| 🔴 | quick-win | **Smallest step to real feedback is deployment, not polish** — run `/council` on the actual work app; this self-review is a smoke test, not market feedback. | — |
| 🟡 | — | **Moat: none, and that's fine** — it's a curated prompt; the durable value is the run → notice a miss → improve → push loop. | — |
| 🟡 | — | **What NOT to build** — a second skill before one is wanted twice, marketplace packaging, or per-language variants. | — |
| 🟢 | quick-win | **Competitor check** — Claude Code ships `/code-review` and `/security-review`; one README sentence on "when to use council vs built-in" prevents the obvious question. | `README.md` |

### Experience *(merged)*

| Sev | Effort | Finding | Where |
|:--:|---|---|---|
| 🟡 | quick-win | **First-run friction (observed live)** — invoked from a multi-project directory, the skill silently assumes cwd is the target; encode the "which app?" question. | `council/SKILL.md` |
| 🟢 | — | **Report-commit guidance missing** — teams will wonder whether to commit the report; one line settles it. | `council/SKILL.md` |
| 🟢 | — | **What works: the Critic** — it's the memorable seat people will quote; don't dilute it with caps or politeness. | `council/SKILL.md` |

### Compliance reviewer

| Sev | Effort | Finding | Where |
|:--:|---|---|---|
| 🔴 | quick-win | **No LICENSE file** — public repo defaults to all-rights-reserved; every install path invites copying into other codebases with no grant. MIT matches intent. | `LICENSE` (none yet) |
| 🟢 | — | **Reverse-flow IP risk** — largely closed by operator setup (personal GitHub never used on the work laptop); residual is the human path only. Awareness, not a convention. | — |

### The Critic (no filter)

A repository containing one 64-line markdown file now has a themed name chosen from a shortlist, a backstory, a README three times longer than the product, a memory entry, and a ceremonial self-review — the infrastructure-to-content ratio is exactly backwards, and the council is currently a government with no citizens. None of today's effort is wasted *if* Monday's plan actually happens: clone it at work, point it at the real app, and let it find one finding that changes what gets built that week. That is the only review of the-council that counts, and it hasn't happened yet. Until a second skill exists, "one source of truth across machines" is an elaborate way to say "a gist with a symlink" — which is fine, as long as nobody here mistakes naming things for shipping things.

---

*Read-only review; no files modified outside this report. Report intentionally left uncommitted — commit or delete per preference.*
